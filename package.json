{
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "your-node-version"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^6.8.3"
  }
}
